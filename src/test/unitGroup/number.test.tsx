/**
 * カスタムユニットグループの数値が正しい要素で構成されているかどうか
 *
 * 【仕様】
 * - コンバーター使用不可
 * - バリデーター使用不可
 * - 検索対象外選択可
 */

import { test, expect } from 'vitest';

test('should pass', () => {
  expect(true).toBe(true);
});
